#include "dfs.h"
#include "bfs.h"

struct Node
{
    void link(Node* n, int distance = 1)
    {
        neighbors.push_back(n);
    }

    std::vector<Node*> neighbors;
};

int main(int argc char** argv)
{
    Node* a;
    Node* b; 
    Node* c; 
    Node* d; 
    Node* e; 
    Node* f; 
    Node* g; 
    Node* h;




    return 0;
}
